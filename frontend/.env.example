# Telegram Login Widget
# Set to your bot's username (without @) to enable the widget on the login page.
# Also register your domain with BotFather via /setdomain.
# On localhost, use an ngrok tunnel and register that domain instead.
VITE_TELEGRAM_BOT_USERNAME=
